<%= stylesheet_link_tag('user') %>
<section class="user">
  <%= image_tag('user.jpg', class:'user_picture') %>
  <div class="content">
    <h2><%= link_to(user.Name, user_path(user)) %></h2>
    <p>
      Number of posts: <%= user.Posts_Counter %>
    </p>
  </div>
</section> 
